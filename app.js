const lessons=[
  {id:'numbers',phase:'Start here',title:'Numbers, units, and variables',time:'18 min',summary:'Learn what a number is allowed to mean before it becomes an input to a model.',body:`<p>AI math starts with an ordinary discipline: never let a number lose its meaning. A model does not see “a house,” “a customer,” or “a sentence.” It sees quantities. A house might become floor area, number of rooms, distance to a station, and previous sale price. A customer might become account age, recent activity, and a support history. Those quantities are useful only when you can say what was measured, in which unit, at what time, and for whom.</p><p>A <strong>variable</strong> is simply a named quantity that can take different values. If <em>x</em> means kilometres travelled, then x = 12 says this example travelled twelve kilometres. That is less mysterious than it looks: the letter is a label on a box. The important part is not the letter; it is the rule that tells you what can go into the box. If you mix kilometres, metres, and miles without recording the unit, a model can fit nonsense very efficiently.</p><div class="path-card"><b>AI situation</b><p>You want to predict delivery time. “Distance” is a variable, but distance alone is incomplete: straight-line distance, road distance, and remaining distance after pickup are different variables. Naming the variable precisely is already part of building the model.</p></div><p>Once quantities have meaning, arithmetic becomes a way to compare change. Addition combines like quantities: 3 minutes plus 4 minutes is 7 minutes. Division often makes a rate: 120 kilometres divided by 2 hours is 60 kilometres per hour. A negative number is not “bad”; it tells you which side of a chosen reference point you are on. Temperature below zero, a balance below zero, and a prediction error below zero each use the same arithmetic but mean different things.</p><div class="equation"><span class="equation-label">First equation</span><div class="equation-main">error = prediction − actual</div><p>If a model predicts 9 days and the delivery takes 11, the error is −2 days. The sign says the prediction was too low; the size says it missed by two days.</p></div><p>Before moving on, get comfortable reading an equation as a sentence. The equals sign does not mean “the answer comes next.” It means the two sides describe the same quantity. You may read the equation above as: the model’s error is the prediction minus what really happened. That reading habit will carry you through every later formula.</p><div class="check"><b>CHECK YOUR MODEL</b><p>For every number, ask: what does it measure, what is its unit, when was it available, and is it allowed to influence this decision? Those questions prevent many “math” errors before any math happens.</p></div>`},
  {id:'vectors',phase:'Represent information',title:'Vectors: many measurements, one example',time:'22 min',summary:'See a vector as one example described by several meaningful quantities.',body:`<p>One number rarely describes an AI problem. To recommend a movie, you may need a viewer’s watch history, language, time of day, and device. To classify an image, you need thousands or millions of pixel values. A <strong>vector</strong> is just a tidy way to keep several numbers together as one object. Think of it as a row on a careful form: every position has an agreed meaning.</p><p>Suppose we describe a delivery with distance, number of items, and rainfall. The vector [8, 3, 1] might mean 8 kilometres, 3 items, and rain present. The order matters. [3, 8, 1] is not the same description if the first position is always distance. This is why AI practitioners talk about <strong>shape</strong>: shape tells you how many examples and how many measurements live in an array. It is a contract, not a technical decoration.</p><div class="flow"><div class="flow-step"><span>01</span><b>Collect</b><small>Measure distance, items, weather.</small></div><div class="flow-step"><span>02</span><b>Order</b><small>Give every position a stable meaning.</small></div><div class="flow-step"><span>03</span><b>Scale</b><small>Keep units comparable when needed.</small></div><div class="flow-step"><span>04</span><b>Use</b><small>Feed one consistent description to a model.</small></div></div><p>Vectors also give us a natural notion of “direction.” If one vector has high distance and many items, while another has low distance and few items, they point toward different kinds of deliveries. A model can learn that some directions in this measurement space usually correspond to delay. That does not make the space magical: it only gives a precise language for comparing multiple quantities at once.</p><div class="equation"><span class="equation-label">Vector notation</span><div class="equation-main">x = [x₁, x₂, x₃]</div><p>Read this as “x is a list of three components.” The subscripts are positions: x₁ is the first measurement, x₂ the second, and so on.</p></div><p>The first useful operation is a weighted sum. Give each measurement a weight that says how strongly it should influence a score, multiply, then add. Distance might matter more than item count; rain might add a small fixed contribution. This is not yet a neural network. It is the core calculation from which much of one is built.</p><div class="check"><b>AI CONNECTION</b><p>An embedding is also a vector. Its coordinates may no longer have friendly names like “distance,” but the same rule survives: similarity and downstream decisions depend on what the coordinates were trained to preserve.</p></div>`},
  {id:'functions',phase:'Build a model',title:'Functions: turn inputs into an output',time:'24 min',summary:'Understand a model as a rule that maps a vector of inputs to a useful output.',body:`<p>A <strong>function</strong> is a rule that takes an input and produces an output. A calculator is a function: enter two numbers, receive their sum. A delivery-time model is also a function: enter a vector describing a delivery, receive a predicted number of minutes. The word “function” is valuable because it forces a question that vague AI language can hide: what exactly goes in, what exactly comes out, and what rule connects them?</p><p>Start with a deliberately simple rule. If x is distance in kilometres, a rough travel-time estimate might be f(x) = 4x + 10. The 4 says each kilometre contributes four minutes; the 10 says there is an initial overhead. This rule will be wrong in many real cases, but it has a clear interpretation. You can ask what happens when distance grows by one kilometre, or whether ten minutes of overhead is plausible.</p><div class="equation"><span class="equation-label">A linear model</span><div class="equation-main">ŷ = w₁x₁ + w₂x₂ + w₃x₃ + b</div><p>ŷ (“y-hat”) is a prediction. The x values are inputs, the w values are learned weights, and b is a baseline offset. The model adds each input after scaling it by its weight.</p></div><p>With several inputs, the same idea becomes a weighted sum. A positive weight means increasing that input tends to increase the score while the other inputs stay fixed. A negative weight means the opposite. This is a description of the model, not a causal claim about the world. If rainy days also have traffic, a weight can exploit that pattern without proving rain caused the delay.</p><p>Neural networks stack many functions. One layer makes intermediate numbers; another layer transforms those; a final layer makes a prediction. The stack can represent more complicated shapes than a single straight-line rule. But every layer is still a function with an input, an output, and parameters. Learning means choosing the parameters so the overall function behaves usefully on examples it has not seen.</p><div class="check"><b>KEEP THE QUESTION SMALL</b><p>Before reading a model formula, say in words what its input and output are. If you cannot say that, the symbols will not become clearer by themselves.</p></div>`},
  {id:'change',phase:'Learn from error',title:'Change, slope, and derivatives',time:'28 min',summary:'Use the idea of “how much does the output move?” before meeting gradient notation.',body:`<p>Training a model is a repeated correction process. First the model makes a prediction. Then we compare it with what actually happened. Finally we change the model’s weights a little in the direction that should reduce future error. The mathematical question is simple to state: if I nudge one weight, how does the error change?</p><p>A graph offers the intuition. Imagine a hill where height means error. Your current location is one choice of model weights. If a tiny step east makes the hill rise, east is a bad direction; if a tiny step west makes it fall, west is promising. The <strong>slope</strong> tells you the local change in height for a small movement. A derivative is the precise version of that slope for a function.</p><div class="equation"><span class="equation-label">Derivative as local change</span><div class="equation-main">d(error) / d(weight)</div><p>Read this slowly: “the change in error with respect to a change in this weight.” A positive value says raising the weight raises error nearby; a negative value says raising it lowers error nearby.</p></div><p>For one weight, you can imagine looking left and right on the hill. Real models have millions of weights, so they live in a space with many directions. The collection of all local slopes is a <strong>gradient</strong>. It points uphill, toward increasing error. Training takes a small step in the opposite direction, downhill. The step must be small enough that the local slope remains useful; a huge step can overshoot into a worse region.</p><div class="flow"><div class="flow-step"><span>01</span><b>Predict</b><small>Run inputs through the current model.</small></div><div class="flow-step"><span>02</span><b>Measure error</b><small>Compare prediction with a target.</small></div><div class="flow-step"><span>03</span><b>Find slope</b><small>Estimate which weights raised error.</small></div><div class="flow-step"><span>04</span><b>Update</b><small>Move weights slightly downhill.</small></div></div><p>The chain rule is how this correction travels through several layers. A later layer affects the final error directly; an earlier layer affects it through everything that comes after. The chain rule multiplies these local effects together so each parameter receives its share of responsibility. Backpropagation is not a separate magic algorithm: it is careful bookkeeping for the chain rule in a layered function.</p><div class="check"><b>AI CONNECTION</b><p>A derivative tells the training procedure how the implemented function responds locally. It does not tell you whether the data, target, or product decision is the right one. A perfectly calculated gradient can optimize the wrong objective.</p></div>`},
  {id:'probability',phase:'Reason under uncertainty',title:'Probability: scores are not decisions',time:'26 min',summary:'Learn why a model’s confidence-like score needs a population, a threshold, and a cost.',body:`<p>Many AI outputs are uncertain. A medical image may be compatible with several conditions. A fraud system may see suspicious signals without knowing intent. A language model may produce a likely next word without knowing whether it is true. Probability gives us a disciplined way to describe uncertainty, but it does not erase it.</p><p>A probability is a number between zero and one attached to a clearly stated event and population. “There is a 0.8 probability of delay” is incomplete until you ask: delay by how much, for which kind of delivery, over what time period, based on which information? The number is not a property floating inside one package. It is a model’s estimate under a particular setup.</p><div class="equation"><span class="equation-label">Conditional probability</span><div class="equation-main">P(delay | distance, rain, items)</div><p>The vertical bar means “given.” Read it as: the probability of delay given the observed distance, rain, and item count.</p></div><p>Even a well-calibrated probability is not yet an action. If a delivery has a 0.3 chance of being late, should you alert the customer? That depends on the cost of a false alert, the cost of silence, available staff, and the customer’s preference. A threshold turns a score into action: alert if the probability exceeds some chosen value. Different costs lead to different thresholds.</p><p>Probability also teaches a useful caution about averages. If a model is right 90% of the time overall, it can still fail badly for a language, a region, a rare condition, or a new time period. A good evaluation separates the population into slices that matter to the decision. The question is not “what is the one score?” but “where, for whom, and under what conditions does this score stay reliable?”</p><div class="check"><b>DECISION RULE</b><p>Never treat “likely” as a command. Pair a probability with its population, calibration evidence, error costs, and a safe fallback for ambiguous cases.</p></div>`},
  {id:'optimization',phase:'Train deliberately',title:'Loss and optimization: choose what gets better',time:'30 min',summary:'Connect examples, error functions, gradients, and validation into one training loop.',body:`<p>A model does not learn directly from a wish such as “be helpful” or “predict well.” It learns from a <strong>loss function</strong>: a numerical rule that says how bad a prediction was on a training example. For a delivery-time prediction, one loss might grow when the predicted minutes differ from actual minutes. For a classifier, a loss might punish the model more when it gives high confidence to the wrong class.</p><p>The loss is a proxy. It translates a real goal into a quantity an optimizer can adjust, and every translation leaves something out. If you optimize only average delivery error, the model can improve the average while being consistently late for a small but important group. If you optimize a chatbot only for fluent text, it can become fluent without becoming accurate. This is why the mathematics of optimization must stay connected to evaluation and product constraints.</p><div class="equation"><span class="equation-label">Gradient descent</span><div class="equation-main">new weight = old weight − learning rate × gradient</div><p>The learning rate controls how far the update moves. The gradient says which direction increases loss, so subtraction moves in the local downhill direction.</p></div><p>Training repeats this update on many examples. A batch is a small group of examples used to estimate a useful update. The loss usually falls on the training data if the process is working, but that is only the first claim. The real question is whether behavior improves on held-out examples that represent future use. This separation is called generalization: fitting the past is not the same thing as working in the next situation.</p><div class="flow"><div class="flow-step"><span>01</span><b>Define</b><small>Choose target, loss, and constraints.</small></div><div class="flow-step"><span>02</span><b>Train</b><small>Update weights on training examples.</small></div><div class="flow-step"><span>03</span><b>Validate</b><small>Test on protected examples and slices.</small></div><div class="flow-step"><span>04</span><b>Decide</b><small>Ship, revise, or reject with evidence.</small></div></div><p>You now have the core mathematics of much supervised learning. Inputs become vectors. A function turns vectors into predictions. A loss measures mismatch. Derivatives and gradients describe local change. Optimization updates weights. Probability and evaluation tell you how much to trust the result and whether an action is justified. Everything advanced—transformers, diffusion models, reinforcement learning—builds on these contracts rather than replacing them.</p><div class="check"><b>TRANSFER CHECK</b><p>When someone says a model “learned,” ask four questions: what were its inputs, what output did it predict, what loss was reduced, and what held-out evidence shows that the improvement transfers?</p></div>`}
];

const app=document.querySelector('#app');let drawer=false;
const route=()=>location.hash.replace('#/','')||'home';
const lesson=id=>lessons.find(item=>item.id===id);
function sidebar(active){const groups=[['Start here',['numbers']],['Represent information',['vectors','functions']],['Learn from error',['change','probability','optimization']]];return `<aside class="sidebar ${drawer?'open':''}" aria-label="Math lessons"><a class="brand" href="#/"><span class="brand-mark">∑</span>Math for AI</a><p class="sub">Begin gently. Build precisely.</p><nav class="lesson-list">${groups.map(([label,ids])=>`<section class="phase"><p class="phase-label">${label}</p>${ids.map(id=>{const item=lesson(id);return `<a class="lesson-link ${active===id?'active':''}" href="#/${id}"><span class="lesson-num">${String(lessons.indexOf(item)+1).padStart(2,'0')}</span><span>${item.title}</span></a>`}).join('')}</section>`).join('')}</nav><p class="sidebar-foot">Each lesson starts with a real AI question before introducing notation.</p></aside>`}
function home(){return `<article class="home"><p class="eyebrow">Beginner path · AI mathematics</p><h1>Math is a language for checking what an AI system is actually doing.</h1><p class="lede">You do not need to arrive knowing equations. Start with quantities that mean something, then learn how several quantities form a vector, how a model maps inputs to outputs, and why gradients can improve that mapping.</p><div class="path-card"><b>HOW TO USE THIS SITE</b><p>Read in order. Do not memorize a formula before you can explain its job in plain language. Every lesson introduces only the notation needed to name a mechanism you have already seen.</p></div><section class="home-grid">${lessons.map((item,index)=>`<a class="home-card" href="#/${item.id}"><span>${String(index+1).padStart(2,'0')} · ${item.time}</span><h2>${item.title}</h2><p>${item.summary}</p></a>`).join('')}</section></article>`}
function page(item){if(!item)return home();return `<article class="lesson"><p class="eyebrow">AI math · lesson ${String(lessons.indexOf(item)+1).padStart(2,'0')} · ${item.time}</p><h1>${item.title}</h1><p class="lede">${item.summary}</p><div class="meta"><span>Plain language first</span><span>Equations introduced slowly</span><span>AI connection included</span></div><div class="prose">${item.body}</div></article>`}
function render(){const id=route();app.innerHTML=`<button class="drawer" aria-label="Open lessons"><i></i><i></i><i></i></button><header class="mobile-bar"><a class="brand" href="#/"><span class="brand-mark">∑</span>Math for AI</a><small>AI foundations</small></header><div class="scrim ${drawer?'show':''}"></div><div class="shell">${sidebar(id)}<main class="main">${id==='home'?home():page(lesson(id))}</main></div>`;document.querySelector('.drawer').onclick=()=>{drawer=true;render()};document.querySelector('.scrim').onclick=()=>{drawer=false;render()};document.querySelectorAll('.lesson-link').forEach(link=>link.onclick=()=>{drawer=false;setTimeout(()=>scrollTo(0,0),0)});scrollTo(0,0)}window.addEventListener('hashchange',()=>{drawer=false;render()});render();
